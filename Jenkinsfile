pipeline {
    agent any
    stages {
        stage('Build Anax'){
            steps {
                sh 'echo "Building anax binaries"'
		sh '''
			#!/usr/bin/env bash
		   	echo $USER
			ls /home/mustafa
		'''
            }
        }
    }
}

